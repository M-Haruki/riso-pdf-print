services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    volumes:
      - ..:/workspace:cached
      - node-modules:/workspace/node_modules
    ports:
      - 5173:5173
    command: >
      sh -c "chown -R node:node /workspace &&
        su node -c 'sleep infinity'"
    environment:
      - NODE_ENV=development
    stdin_open: true
    tty: true

volumes:
  node-modules: